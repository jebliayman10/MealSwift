import { StubScreen } from "@/components/StubScreen";

export default function ProfilePage() {
  return (
    <StubScreen
      active="profile"
      title="Profile"
      sub="Account, language, and saved recipes. Coming next."
    />
  );
}
