import { StubScreen } from "@/components/StubScreen";

export default function ExplorePage() {
  return (
    <StubScreen
      active="explore"
      title="Global Explore"
      sub="3D globe coming soon — spin, stop, glimpse a region, tap to dive in."
    />
  );
}
